{
  "name": "tam-backend",
  "version": "1.0.0",
  "description": "Backend API for Tam Alarabiya website (categories, gallery images, dashboard).",
  "main": "src/server.js",
  "scripts": {
    "dev": "nodemon src/server.js",
    "start": "node src/server.js",
    "start:prod": "node src/server.js",
    "verify": "node scripts/verify-backend.js",
    "seed:company-profile": "node seed-company-profile.js",
    "seed:page-content": "node scripts/seed-page-content.js",
    "seed:categories": "node -e \"require('dotenv').config(); const connectDB=require('./src/config/db'); const {seedCategories}=require('./src/services/categorySeedService'); const m=require('mongoose'); connectDB().then(async()=>{ console.log(await seedCategories()); await m.disconnect(); });\""
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "cloudinary": "^2.9.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "helmet": "^7.0.0",
    "jsonwebtoken": "^9.0.3",
    "mongoose": "^8.5.0",
    "morgan": "^1.10.0",
    "nodemailer": "^8.0.11",
    "qrcode": "^1.5.4"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
